// 1. Import
import { Icon } from '@chakra-ui/react'
import { GrCart } from 'react-icons/gr'

// 2. Use the `as` prop
function Example() {
  return <Icon as={GrCart} />
}
export default Example;